<!DOCTYPE html>
<html>
<head>
  <meta name=”viewport” content=”width=device-width, initial-scale=1.0″ >
  <title>Workshoply</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= csrf_meta_tags %>

  <link href="https://fonts.googleapis.com/css?family=Lato|Lustria|Roboto|Merriweather" rel="stylesheet">

</head>

<body>

<!--Nav bar -->
<nav class="navbar navbar-default navbar-static-top navbar-custom">
  <div class="navbar-custom">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag("workshoply-logo.png", alt: "Workshoply"), '/', class: "navbar-brand navbar-logo" %>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Workshops", workshops_path %></li>
        <% if current_user %>
          <li><%= link_to "Post a Workshop", new_workshop_path %></li>
          <li><a class="dropdown-toggle" data-toggle="dropdown" href="#"><%= current_user.full_name.titleize %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", user_path(current_user) %></li>
              <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><a href="/about">About</a></li>
          <li><%= link_to "Sign Up", signup_path%></li>
          <li><%= link_to "Sign In", login_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<!-- Devise Flash MSG -->
<div class="notice-error-msg">
  <% if notice.present? %>
    <div class="alert devise-msg <%= params[:controller] == 'plans' ? 'alert-warning animated bounceInUp' : 'alert-info' %>">
      <a aria-label="close" class="close" data-dismiss="alert" href="#" style="padding-top: 10px">&times;</a>
      <p>
        <%= notice %>
      </p>
    </div>
  <% elsif alert.present? %>
    <div class="alert alert-danger devise-msg">
      <a aria-label="close" class="close" data-dismiss="alert" href="#" style="padding-top: 10px">&times;</a>
      <p>
        <%= alert %>
      </p>
    </div>
  <% end %>
  <% flash.each do |name, msg| %>
    <% if name == 'error' %>
      <div class="notice-error-msg">
        <div class="alert remind-msg alert-danger">
          <a aria-label="close" class="close" data-dismiss="alert" href="#" style="padding-top: 10px">&times;</a>
          <p>
            <%= msg %>
          </p>
        </div>
      </div>
      <% end %>
      <% end %>
    </div>
    <!-- end Devise Flash MSG -->
    <%= yield %>

    <!-- rails js files -->
    <%= javascript_include_tag 'application' %>

    <!-- geocomplete -->
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDdEksj2K9pX7vBO8_jZgjFZk4jvWQQI0o&libraries=places"></script>
  </body>
</html>